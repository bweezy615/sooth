[project]
name = "pick-engine"
version = "0.1.0"
description = "Multi-sport prediction engine with a cryptographically committed public record"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2",
    "numpy>=1.26",
    "scikit-learn>=1.5",
    "requests>=2.32",
    "pyarrow>=17.0",
]

[tool.uv]
package = false
