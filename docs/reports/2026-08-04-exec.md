# Sooth — executive run log

## 2026-08-04 · run 1 (inline, operator present)

### Board health — GREEN

```
capture cron   schedule success 2026-08-04T04:15:09Z
               schedule success 2026-08-04T03:36:50Z
               schedule success 2026-08-04T00:58:04Z
               schedule success 2026-08-03T23:18:14Z
board.json     generated 2026-08-04T04:15Z · 1.1h old · FRESH
               15 events · boards: MLB · in season: MLB, NFL, NHL, UFC
               served live at https://bweezy615.github.io/sooth/data/board.json
credits        7,567 remaining (reserve floor 3,000)
```

Four consecutive unattended cron runs. Nothing to escalate.

**Credit projection to NFL Week 1 (2026-09-09):** ~4 credits per sweep at the
current in-season set, 8 sweeps a day, so roughly 32/day. 36 days to kickoff is
about 1,150 credits, leaving well over 6,000 in hand when the season opens.
Burn rate clears the deadline with a wide margin. No action needed.

### Work completed — operator request, ahead of the queue

The board on the homepage was truncated to the top 7 sides. Now renders every
side on the slate inside its own scroll region, so a long slate cannot blow out
the dashboard shell.

Verified in the browser, not assumed:

```
rowsRendered   30
count label    "MLB · 15 games · 30 sides"
scroller       430px, scrollable: true
body overflow  false
first row      San Diego Padres · best at Caesars
last row       San Francisco Giants · best at Caesars
```

Impeccable detector: 0 findings.

### Deferred to the user

- **$19/mo is still an invented price behind a mailto.** Nothing can be sold
  until P3 lands, and P3 stops at test mode by charter.
- **`hi@sooth.co` does not receive mail.** No domain yet.

### Notes for the next run

P1 (NFL readiness) cannot be verified end to end yet: Week 1 is 36 days out and
the collection window is 36 hours, so no NFL game enters the board by design.
Verifying it early means temporarily widening the window to pull a real NFL
slate, confirming the pipeline handles it, and *not* committing the wide window
as the default. That costs about 1 credit. Do it on a branch.

Nothing in this run contradicts an earlier finding.

---

## 2026-08-04 · run 3 — P2 line-movement alerts: DONE

### Board health — GREEN
```
cron     schedule success 2026-08-04T04:15:09Z (and 03:36:50Z)
board    1.2h old · 15 events · FRESH · served live
credits  7,563 remaining (floor 3,000) · 0 spent this pass
```

### Built

`engine/alerts.py`. Two detectors, deliberately separate because they are not
the same signal:

- **drift** — one book's own price moved between our observations. That is the
  book changing its mind.
- **divergence** — a book's current price sits away from the cross-book median.
  That is the one that pays immediately.

Both measured in points of implied probability, never raw American odds, which
are non-linear and discontinuous across ±100. Provenance-gated to `own_capture`
exactly like CLV: `espn_open` is someone else's claim about a price we never
watched.

### Verification — real history, output pasted

```
observations   : 2386
threshold      : 1.5 pts of implied probability
drift alerts   : 2
divergence     : 0
  DRIFT  New Orleans Saints at Detroit Lions   DraftKings shortened 2.27 pts on over (-105 to -115)
  DRIFT  New Orleans Saints at Detroit Lions   DraftKings drifted out 2.27 pts on under (-115 to -105)
```

Those two alerts are the two sides of one coherent move: DraftKings shifted the
juice on the NO at DET total. The symmetry is the arithmetic checking itself.

```
46 passed in 3.69s
```

12 new tests. Fires on a real move, silent on a one-cent tick, catches a slow
walk at the step that crosses the threshold, never compares across books,
requires 3+ books before claiming a consensus, ignores a book paying *less*
than consensus (a worse price is not an opportunity), uses each book's latest
price, and refuses non-`own_capture` rows entirely.

### Finding — divergence cannot fire yet, and that is not a bug

Zero divergence alerts because **our capture history is single-book**. ESPN
gives DraftKings only, and divergence needs at least three books to have a
consensus worth diverging from.

The multi-book data already exists — `engine/lines.py` reads ten books — but it
writes `board.json`, not capture rows, so the alert detector never sees it.

**Deferred to the user, because it costs credits:** wiring `engine/lines.py` to
also append its multi-book quotes into `data/capture/` would make divergence
live immediately and would make CLV multi-book at the same time. Roughly 1
credit per sport per sweep on top of current burn. This is the single highest
-value unblock in the queue and I did not take it unilaterally.

Nothing in this pass contradicts an earlier finding.
