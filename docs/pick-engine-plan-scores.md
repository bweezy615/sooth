# Synthesis scores

A (product-first, Desk Instrument): honest 10/10 — paywall never touches the proof (picks decay to public at kickoff), losses styled like wins, positioning pass ships in the same deploy; shippable 8/10 — zero model risk, all work is surface+automation the codebase already supports (verified: readPro exists, subscribe.html re-arm instructions at lines 101–107 exist, replay_grade.py exists); differentiating 10/10 — the time-decay paywall ("Pro buys timing, not wins") is the only design no tout can copy and is recognizably SOOTH; $20-efficient 9/10 — defaults to $0, prices the real $30 option honestly. TOTAL 37.

B (model-first, tuned blend): honest 10/10 — CIs on everything, publish-whatever-comes-out; shippable 7/10 — walk-forward tuning + leakage tests + live-path QB feature is a week of engine risk for a Brier improvement that cannot change the headline claim ("still loses"), and it ships NO revenue path; differentiating 6/10 — a blend that mostly reproduces the market is invisible product-side; the simpler page is less distinctive; $20-efficient 9/10 — $0, correctly defers the Odds API month until it unlocks the line-delta backtest. TOTAL 32.

C (data-first, buy the sharp line): honest 8/10 — strong header copy, but ships a new model feature (line_delta) backtested against data purchased days earlier, tight enough to invite a rushed claim; shippable 6/10 — most overloaded (buy + bulk backfill + edge engine + model feature + gate + page + crons in ~12 days) and its "new engine.html" collides with the existing live Engine Room page; differentiating 8/10 — the 40-book Pinnacle-anchored spectrum is great desk material; $20-efficient 6/10 — commits $30 unconditionally, 150% of budget, before any conversion signal. TOTAL 28.

WINNER: A. Grafts taken: from B — the iron rule that no published number exists outside published_figures.py, a divergence-decile table with binomial CIs (computed on the EXISTING independent-vs-market divergence, no model change needed), the explicit slips-if-tight cut order, and the deferred line-movement-feature note; from C — the credit-budgeted resumable backfill_bulk.py design (19K hard stop) and the Pinnacle de-vig fair anchor on the spectrum as the two concrete uses of the optional $30 month, plus the "ask for their merkle root" closer line.
